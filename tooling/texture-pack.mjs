#!/usr/bin/env node
/**
 * Placeholder for TexturePacker CLI integration.
 * Usage: texturepacker --format phaser3 --sheet ../assets/atlases/characters.png ...
 */
console.log("Export atlases with TexturePacker → assets/atlases/");
console.log("Then update BootScene to load.atlas() instead of generateTexture.");
